{
  "name": "tfjs-issue-5584-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "Apache 2.0",
  "scripts": {
    "compile": "tsc",
    "build": "yarn compile && esbuild --bundle index.js --outdir=dist/",
    "test": "yarn build && node dist/index.js"
  },
  "devDependencies": {
    "@tensorflow/tfjs-core": "^3.9.0",
    "esbuild": "^0.12.26",
    "typescript": "^4.4.2"
  }
}
